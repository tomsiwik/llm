.env
__pycache__/
*.pyc
.worktrees/

# Large model files (keep local)
adapters/*/adapter_model.safetensors
adapters/*/tokenizer.json
adapters/*/tokenizer_config.json
*.safetensors
*.gguf
*.bin
*.npz
*.pt
*.pth
*.h5
*.onnx
*.pkl
*.pickle

# Training data (keep local)
data/distillation/*/train.jsonl
data/distillation/*/train_split.jsonl
data/distillation/*/val.jsonl

# Training datasets (reproducible via HF datasets / scripts)
*.parquet
*.jsonl
micro/**/data/
results/**/data/
results/**/checkpoint-*/

# Results data (keep local)
results/*.jsonl
results/*.csv

# SQLite transient files (never commit)
*.db-wal
*.db-shm

# Large experiment results (some contain full hidden states/embeddings)
micro/**/results.json

# Paper2code work directory
.paper2code_work/

# OCR traineddata
eng.traineddata

# Screenshots
screenshots/

# SQLite databases
*.db

# Node modules
node_modules/
bun.lock

# Ralph runtime state
.ralph/loop.lock
.ralph/agent/
.ralph/events-*.jsonl
.ralph/current-events
.ralph/current-loop-id
.ralph/diagnostics/
# uv.lock — committed for reproducible installs
.archive/
*.npy
